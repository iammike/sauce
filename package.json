{
  "name": "sauce",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "serve": "python3 -m http.server 8000",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "jsdom": "^28.1.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.12 <23 || >=24"
  }
}
