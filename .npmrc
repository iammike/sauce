# engines is advisory by default, so a below-floor Node installs fine and only
# fails later as ERR_REQUIRE_ESM from deep inside jsdom, which names the symptom
# rather than the cause. Fail at install time instead, where the message says
# which Node version is required.
engine-strict=true
